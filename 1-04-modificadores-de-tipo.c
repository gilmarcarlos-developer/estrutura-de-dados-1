#include <stdio.h>
#include <stdlib.h>

int main(){
    
    unsigned short int u;
    int x = 10;
    double y;
    //sizeof mostra quando bytes a variavel está ocupando
    printf("%i -> variavel u\n", sizeof(u));
    printf("%i -> variavel x\n", sizeof(x));
    printf("%i -> variavel y\n", sizeof(y));    
    
    return 0;
}
